import { PropsWithChildren } from 'react';
import type { FormProps } from './FormRenderer.types';
export declare const Form: ({ children, id, sx, disabled, onChange, }: PropsWithChildren<FormProps>) => JSX.Element;
