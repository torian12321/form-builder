import type { Meta, StoryObj } from '@storybook/react';
import { LoadingBar } from '.';
declare const meta: Meta<typeof LoadingBar>;
export default meta;
type Story = StoryObj<typeof meta>;
export declare const Base: Story;
