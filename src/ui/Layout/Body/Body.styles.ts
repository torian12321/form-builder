import { styled } from '@mui/material/styles';

export const Wrapper = styled('main')({
  position: 'relative',
  padding: 16,
  margin: 'auto',
  display: 'flex',
  flexDirection: 'column',
  flexGrow: 1,
  overflowX: 'auto',
});
